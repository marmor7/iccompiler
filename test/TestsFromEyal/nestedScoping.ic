class B{
	int member1;
	int member2;
	boolean member3;
	void foo(){
		this.member1 = 1;
		this.member2 = 2;
		this.member3 = (member1 >= member2);  // false
	}
}

class A {

	static int f(int i,int j){
		return (i+j)*(i-j);	
	}
	
	static boolean g(int k1,int k2){
		return (k1 == 2*(k2+13));
	}
	
	static int sign(int n){
		if (n > 0){
			return 1;
		}	
		else{
			int k = n;
			{
				if (k == 0){
					return k; //0
				}
			}
		return -1;
		}
	
	}
	
	static void func1(int k1){
		int k2 = k1 * k1; // k2 is now non-negative.
		{
			boolean k2 = true;
			{
				int k2 = 0;
				while (k2 < 0){
					Library.println("mistake");
				}
			}
		}
	}
	
	static void main(string[] args){
		int i = 0;
		int j = 12;
		int k = 3 * j;	// 36
		{
			int i = 1;
			int j = 1;
			int k = j; // 1
			{
				boolean k = true;
				if (k){
					int i = 2; 
					j = i+1; // 3
				}
			}
		}
		if (j>0){
			i = 2;
			j = 15;
		}
		j = 0 - 2; // j == -2
		Library.printi(i + j); //0
		Library.println("");
		Library.printi(i); //2
		Library.println("");
		Library.printi(j); //-2
		Library.println("");
		//                       0
		Library.printi(A.f(i,A.f(i,j)));  //4
		Library.println("");
		Library.printb(A.g(i,j));   //false
		Library.println("");
		Library.printb(A.g(26,A.f(0,0)));  //true
		Library.println("");
		Library.printi(A.sign(i));   //1
		Library.println("");
		Library.printi(A.sign(j));   //-1
		Library.println("");
		Library.printi(A.sign(0));	 //0
		A.func1(12);		
		A.func1(-20000);
		A.func1(0);
		
		B b = new B();
		b.foo();
		{
			int b = 23;
			Library.println("");
			Library.printi(b); //23
		}
		Library.println("");
		Library.printi(b.member1);
		Library.println("");
		Library.printb(b.member3);
	}
}