class T {

	static void main(string[] args){
		boolean b1;
		boolean b2;
		boolean b3;
		boolean b4;
		int a1 = 2;
		int a2 =0;
		int a3=0;
		int a4=0;
		int[] a = new int[5];
		b1 = true;
		b2 = b1;
		b3 = (!(!(!b2))); // b3 is false
		b4 = b1 || (b2 && b3); // b4 is true
		
		//a1= 2;
		//a2 = (2+a1+2+a1)*a1; //a2 is 16
		a3 = -(a2/a1); // a3 is -8
		a4 = 6 % 3; // a4 is 1.
		
		Library.printb(b1);
		Library.println("");
		Library.printb(b2);
		Library.println("");
		Library.printb(b3);
		Library.println("");
		Library.printb(b4);
		Library.println("");
		Library.printi(a1);
		Library.println("");
		Library.printi(a2);
		Library.println("");
		Library.printi(a3);
		Library.println("");
		Library.printi(a4);
		Library.println("");
		Library.printi(a.length);
	}
	
}